{
  "name": "wedding-invitation-backend",
  "version": "1.0.0",
  "description": "wedding invitation backend",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc --watch & nodemon --env-file=.env dist/app.js",
    "start": "tsc && node -w .env --env-file=.env dist/app.js",
    "build-clean": "tsc --build --clean"
  },
  "author": "Kamal Shafi",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "typescript": "^5.5.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^20.14.8",
    "nodemon": "^3.1.4"
  }
}
